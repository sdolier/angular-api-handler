{
  "name": "angular-api-handler",
  "version": "0.0.1",
  "description": "An angular api handler for intercepting http requests",
  "scripts": {
    "ngc": "ngc",
    "build": "rm -rf aot dist && npm run ngc && node ./scripts/copy-package",
    "buildDemo": "npm build && node ./scripts/copy-package-to-demo.js",
    "publishPackage": "npm run build && cd dist && npm publish"
  },
  "dependencies": {
    "@angular/core": "^2.2.1",
    "@angular/common": "^2.2.1",
    "@angular/compiler": "^2.2.1",
    "@angular/compiler-cli": "^2.2.1",
    "@angular/platform-browser": "^2.2.1",
    "rxjs": ">=5.0.0-beta.0"
  },
  "devDependencies": {
    "@types/node": "^6.0.41",
    "copy-dir": "^0.3.0",
    "typescript": "^2.0.3"
  }
}
